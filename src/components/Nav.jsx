import React from 'react'
import './Nav.css'

export default (props) => {
    return (
        <nav className='nav'>
            <a href="#" className="notification">
                <span>Lista</span>
                <span className="badge">3</span>
            </a>
        </nav>
    )
}

